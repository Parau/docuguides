## Docusaurus (original)
  `npm start`
    Starts the development server.

  `npm run build`
    Bundles your website into static files for production.

  `npm run serve`
    Serves the built website locally.

  `npm run deploy`
    Publishes the website to GitHub pages.

## pnpm
pnpm dev:atual
- Olhar o package.json para ver qual o projeto atualmente configurado

pnpm install
- Depois que faz a cópia para iniciar um novo projeto.

pnpm add -D sass --filter ondg-planos
- Para instalar como (devDependencies) somente no cdptbr

pnpm add @next/third-parties --filter ondg-planos
- Para instalar como (dependencies) somente no cdptbr

pnpm remove <nome-do-pacote>
- Excluir pacote do projeto

pnpm why react-dom -r --depth=Infinity
- Verificar as dependências do pacote no projeto

pnpm add react-lite-youtube-embed --filter ondg-planos
pnpm remove react-player --filter ondg-planos


## auxiliares do dia a dia
importancia-nome-alfabetizacao
chatgpt-iel

turbo run build --filter chatgpt-iel
pnpm add docusaurus-plugin-dotenv --filter chatgpt-iel
